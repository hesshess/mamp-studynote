    // $test = "SELECT p.id, p.title, p.content, p.created, p.writer, c.name FROM post p, category c WHERE p.category_id = c.id LIMIT %d , %d ";
    // $sql2 = sprintf("SELECT p.id, p.title, p.content, p.created, p.writer, c.name FROM post p, category c WHERE p.category_id = c.id LIMIT %d , %d ", $start, $list_num);
    // $sql3 = sprintf($test, $start, $list_num); 